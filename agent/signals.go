package agent

func (a *Agent) startSignalHandler() error {
	// start a go routine to handle signals
	return nil
}

// Runs the signal handler
func (a *Agent) signalHandler() {
}

// Terminates the running signal handler
func (a *Agent) stopSignalHandler() error {
	return nil
}
